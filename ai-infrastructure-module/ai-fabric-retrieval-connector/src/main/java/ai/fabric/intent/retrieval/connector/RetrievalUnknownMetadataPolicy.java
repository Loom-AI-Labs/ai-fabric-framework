package ai.fabric.intent.retrieval.connector;

/**
 * Handling for external metadata paths that are not explicitly allowlisted.
 */
public enum RetrievalUnknownMetadataPolicy {
    DROP,
    REJECT
}
