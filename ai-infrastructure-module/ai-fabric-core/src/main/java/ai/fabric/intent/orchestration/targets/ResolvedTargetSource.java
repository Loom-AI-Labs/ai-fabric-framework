package ai.fabric.intent.orchestration.targets;

/**
 * Origin for a resolved target reference.
 */
public enum ResolvedTargetSource {
    REQUEST_ATTACHMENTS,
    SESSION_METADATA,
    WORKING_SET,
    ACTION_RESULT_ITEMS
}
