ai-config:
  default-embedding-model: text-embedding-3-small
  default-search-limit: 10

ai-entities:
  document:
    indexing:
      enabled: true
      max-characters: 8000

  product:
    indexing:
      enabled: true
      max-characters: 8000

  medical-case:
    indexing:
      enabled: true
      max-characters: 8000

  transaction:
    indexing:
      enabled: true
      max-characters: 8000

  candidate:
    indexing:
      enabled: true
      max-characters: 8000
